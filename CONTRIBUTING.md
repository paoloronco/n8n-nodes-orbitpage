# Contributing

Use Node.js 22 or newer. Install with `npm ci`, then run `npm run check` before
opening a pull request. Keep runtime dependencies empty, preserve input item
linking, avoid logging credentials or response bodies, and update the operation
catalog tests whenever the OrbitPage OpenAPI contract changes.
